<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile Show Page</title>
  </head>
  <a href="/">Home</a>
  <a href="">Sign out</a>
  <body>
    <h1><%= profile.name %></h1>
    <h1><%= profile.age %></h1>
    <h1><%= profile.breed %></h1>
    <h1><%= profile.likesAndDislikes %></h1>
    <h1><% if (profile.friendlyToStrangers) { %>
        <p>This Woof is friendly to strangers!!</p>
      <% } else { %>
        <p>This woof is not friendly yet!</p>
      <% } %></h1>
      <form action="/profile/<%=profile._id%>?_method=DELETE" method="POST">
        <button type="submit">Delete <%= profile.name %></button>
      </form>
      <a href="/profile/<%= profile._id %>/edit">Edit <%= profile.name %></a>
  </body>
</html>